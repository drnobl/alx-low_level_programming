#include "main.h"
#include <stdio.h>

/**
 * _abs - check the code
 *@ab: Int ab prints the abolute value
 * Return: Always 0.
 */
int _abs(int ab)
{
	if (ab >= 0)
	{
		return (ab);
	}
	return (-ab);
}

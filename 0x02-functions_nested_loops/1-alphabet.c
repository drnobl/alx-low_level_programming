#include "main.h"

/**
 * print_alphabet - Prints the alphbets in lowercase
 *
 * Return: No return
 */
void print_alphabet(void)
{
	int a;

	for (a = 'a' ; a <= 'z' ; a++)
		_putchar(a);
	_putchar('\n');
}

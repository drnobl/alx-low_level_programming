#include <stdio.h>
/**
 * main - Entry point for Hello program using printf
 *
 * Return: Return is always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
